set nohelp
set backup
set backupdir /var/backup

#set multibuffer
set locking
set afterends
set tabsize 3
set mouse
set linenumbers
set trimblanks
set autoindent
set atblanks
unset wordbounds
set softwrap
unset casesensitive
unset constantshow
set wordchars "_"
# set regexp
set smarthome
set suspendable
set matchbrackets "({[<")}]>""
set historylog
set positionlog
set zap
set speller ispell
set indicator
set minibar

#set titlecolor bold,brightred
set titlecolor bold,brightwhite
set statuscolor cyan
set keycolor cyan
set functioncolor brightblack
set numbercolor bold,brightblack
set selectedcolor bold,black,brightwhite
set scrollercolor brightblack

bind ^/ comment main
# ^H = ctrl-backspace
bind ^H cutwordleft all
bind ^] findbracket all
bind ^D cutwordright all
bind ^R replace main
bind ^F whereis all
bind ^C casesens all
bind ^S casesens all
bind ^R replace all
bind ^R regexp replace
bind ^R regexp search
bind ^B backwards all
bind ^Z undo all
bind ^Y redo all
bind ^Space complete all
bind ^E execute all
bind ^P flippipe all
bind ^G gotoline all
bind ^F browser insert
bind ^E flipexecute all
bind ^E execute main
bind ^N flipnewbuffer all
bind ^N findnext main
bind ^T mark main
bind ^P findprevious main
bind ^O insert all
bind ^W writeout all
bind ^S savefile main
bind ^B end all
bind ^A home all
bind M-F formatter all
bind M-L linter all
bind M-S speller all
bind M-Z suspend all
bind M-I whitespacedisplay all
bind M-W softwrap main
bind M-H nohelp all
bind M-R recordmacro all
bind M-X runmacro all
bind M-3 linenumbers all
bind M-? help all
bind M-( "()[D" main
bind M-{ "{}[D" main
bind M-9 "co" main
bind M-8 "headerf" main
bind M-7 "importf" main
bind M-6 "function() end[D[D[D[D[D" all
bind M-5 "|lv" main
bind M-3 linenumbers main

bind ^Q cancel all



bind ^Q exit main
bind ^X exit main

bind M-A anchor main
bind M-N nextanchor main
bind M-P prevanchor main

# for older nano versions:
# bind ^C copytext main
# bind ^V uncut all
# for newer nano versions:
bind ^C copy main
bind ^V paste all

# enable advanced syntax highlighting
m4_esyscmd((ls -1 nano-syntax; echo hexproc.nanorc) | sed -E 's:^(.*)$:include /etc/nanorc.d/\1:')
include "/usr/share/nano-syntax-highlighting/python.nanorc"
include "/usr/share/nano-syntax-highlighting/glsl.nanorc"
include "/usr/share/nano-syntax-highlighting/yaml.nanorc"
